# Evitar que archivos innecesarios se copien al contenedor
# y rompan el cache de Docker

# Node modules del host (importante para evitar conflictos)
node_modules

# Git
.git
.gitignore

# Archivos de Docker
Dockerfile
docker-compose.yml
.dockerignore

# IDEs
.vscode
.idea

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Build outputs (en caso de que haya builds locales)
dist

